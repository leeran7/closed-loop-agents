{
  "defaultMode": "bypassPermissions",
  "permissions": {
    "allow": [
      "Bash(pnpm vitest run *)",
      "Bash(pnpm tsc --noEmit *)",
      "Bash(pnpm vitest *)",
      "Bash(npx tsc --noEmit *)",
      "Bash(pnpm test *)"
    ]
  }
}
